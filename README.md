Slider 


Slider can move by pressing the left or right arrow

```
 let firstSlider = new Slider('.slider', {
        //      currentSlide: 1, по умолчанию 0
        speed: 1500, //в ms по умолчанию 1000
        //animationType: 'linear', по умолчанию ease

        //      autoplay: true, по умолчанию false
        //      autoplayDirection: 'left', //по умолчанию right
        //      autoplaySlideTime: 4000, //ms

        //      dotsEnable: false, //  По умолчанию true
        //      arrowsEnable: false, Если не указывать, то по умолчанию будет true
        //      dotsHeight: '12px',
        //      dotsWidth: '12px'
    });
```
